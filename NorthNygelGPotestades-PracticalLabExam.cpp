#include <iostream>
using namespace std;

int main(){
	int numbers[] = {5, 4, 3, 2, 1};
	int length = size(numbers);
	cout << "The length of the array is: " << length << "." << endl;
	return 0;
};
